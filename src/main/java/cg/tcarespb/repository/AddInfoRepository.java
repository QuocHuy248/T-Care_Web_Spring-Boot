package cg.tcarespb.repository;

import cg.tcarespb.models.AddInfo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AddInfoRepository extends JpaRepository<AddInfo,String> {

}
